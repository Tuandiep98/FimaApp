class BackgroundSyncManager {
  static Future backgroundSyncs({
    bool syncDeviceStatus = true,
  }) async {
    // var transactionService = locator<ITransactionService>();
    // await transactionService.pushTransactionBackground();
  }
}
